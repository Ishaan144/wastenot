# Copy this file to ".env" and paste in your own key to enable AI Chef.
# Free key: https://aistudio.google.com → "Get API key"
# Without a key the app still works — it falls back to built-in recipe suggestions.
EXPO_PUBLIC_GEMINI_API_KEY=your_key_here
